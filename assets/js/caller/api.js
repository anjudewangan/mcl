const baseUrl = window.origin;

const caller_api = `${baseUrl}/newComplain`;

const employee_api = `${baseUrl}/employee_data`;

const addTech_api = `${baseUrl}/addTech`;

const excalated_matrix_api = `${baseUrl}/escalated_matrix`;

const caller_data_api = `${baseUrl}/caller_data`;

const admin_api = `${baseUrl}/admin_data`;

const updateTech_api = `${baseUrl}/updateTech`;

const escalated_complains_api = `${baseUrl}/complain/escalated`;

const jobs_api = `${baseUrl}/jobs`;

const complain_count_api = `${baseUrl}/complains`;

const contact_us_api = `${baseUrl}/contactUs`;

const pagination_api = `${baseUrl}/pagination`;

const search_api = `${baseUrl}/search`;

const update_password_api = `${baseUrl}/update-password`;

const update_password_escalated_matrix = `${baseUrl}/update-password-escalated-matrix`;

const update_employee_api = `${baseUrl}/update-employee`;
